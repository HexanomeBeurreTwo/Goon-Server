var api = require('../app.js'),
request = require('supertest')(api);

describe('Activity Route', function() {
  describe('#getActivities()', function () {
    it('should return right json string', function (done) {
    	// request
     //    	.get('/activities')
     //        .expect(200, done);
    });
  });

  describe('#addActivity()', function () {
    it('should return right json string', function (done) {
    	// Fill with what you should send and receive
    	//url : /activity
    });
  });

  describe('#updateActivity()', function () {
    it('should return right json string', function (done) {
    	// Fill with what you should send and receive
    	//url : /activity/:id
    });
  });

  describe('#deleteActivity()', function () {
    it('should return right json string', function (done) {
    	// Fill with what you should send and receive
    	//url : /activity/:id
    });
  });

  describe('#getActivity()', function () {
    it('should return right json string', function (done) {
    	// Fill with what you should send and receive
    	//url : /activity/:id
    });
  });
});
